;; -*- mode: emacs-lisp; lexical-binding: t -*-

(source gnu)
(source melpa)

(package-file "PKG.el")

(files "*.el" "test/*.el")

(development
 (depends-on "buttercup")
 (depends-on "package-lint")
 (depends-on "undercover"))
